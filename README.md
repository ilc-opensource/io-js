* Install CppHeaderParser

``` bash
sudo apt-get install python-pip
sudo pip install CppHeaderParser
sudo pip install ply
sudo pip install termcolor
```

* Run autogen

``` bash
cd utils/autogen
./run.py
```
